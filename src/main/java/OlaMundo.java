public class OlaMundo {
    
    // Variáveis
  
    /*
    
     Quero adicionar ao software uma breve descrição sobre mim
     Eu tenho um nome, idade, uma altura.
    */
    
    // Declaração de variável
    String nome;
    
   // Inicialização de variável

    /* Operadores artiméticos 
    Subtração (-)
    Adição (+)
    Multiplicação (*)
    Divisão (/)
    Resto(%)  // Retorna o resto da divisão
    
    */
    
    /*
    Operadores de atribuição
    
    int a = 1
    
    int a += 5  a  = a + 5
    int a -= 5  a  = a - 5
    int a *= 5  a  = a * 5
    int a /= 5  a  = a / 5
    */
    
    /*
    Operadores de comparação
    int a = 1
    int b  = 2
    if(a == b) operador de igualdade
    if(a != b) operador de diferença
    if(a > b) operador de maior que
    if(a < b) operador de menor que
    if(a <= b) operador de menor igual 
    if(a >= b) operador de maior igual 




    
    */
    
        
    
    public static void main(String[] args) {
       String nomeCompleto = "Danilo de Souza Miguel";
       int anoAtual = 2022;
       int anoNascimento = 1985;    
       int idade = anoAtual - anoNascimento;
       double altura = 1.78;

        System.out.println("Olá Mundo");
        System.out.println(nomeCompleto);
        System.out.println(idade);
        System.out.println(altura);


        
    }
}
